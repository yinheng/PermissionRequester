apply plugin: 'java'

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])

    compile project(':permission-requester-annotation')
    compile 'com.google.guava:guava:20.0'
    compile 'com.squareup:javapoet:1.7.0'
}

